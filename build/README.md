```
$ cd kiosk-kit/build/

# If not done already:
$ npm install

# Run the default Gulp task:
$ npm run gulp

# Update your bower.json and run:
$ npm run bower
# Use the main-bower-files override option to control what gets copied.
# Don't forget to symlink your files!
```
